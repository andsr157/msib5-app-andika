export interface FeatureType {
  title: string
}

export interface ProductType {
  title: string
  type: string
  price: number | string
}

export interface FooterLinkType {
  heading: string
  links: []
}
